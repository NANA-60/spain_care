import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF4BA8A6); // لون التطبيق الرئيسي
  static const secondary = Color(0xFF8CCCCC);

  static const white = Colors.white;
  static const white2 = Colors.white70;

  // Dark (مرجع)
  static const darkBackground = Color(0xFF0B0F12); // اخترت داكن أغمق للـ background
  static const darkCard = Color(0xFF151618); // كارد داكن
}
